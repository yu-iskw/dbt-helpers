# It was generated by dbt-helper==0.1.0.
version: 2

sources:
  - name: main_intermediate
    database: "{{ var('projects')['main_intermediate'] }}"
    tables:
      # dbt source ID: `source("main_intermediate", "main_intermediate__int_users")`
      - name: main_intermediate__int_users
        identifier: int_users

        description: |
          Raw view int_users

        config:
          # see also https://docs.getdbt.com/reference/resource-properties/freshness
          freshness:
            warn_after:
              count: 24
              period: hour # minute | hour | day
            error_after:
              count: 36
              period: hour # minute | hour | day
            # filter: "DATE(event_time) BETWEEN (CURRENT_DATE() - 1) AND CURRENT_DATE()" # if partition table
          tags:
            - raw_data

          # meta will be written into BigQuery labels by dbt-helper.
          meta:
            labels:
              owner: test_team

        columns:
          - name: id
            description: ""
          - name: name
            description: ""

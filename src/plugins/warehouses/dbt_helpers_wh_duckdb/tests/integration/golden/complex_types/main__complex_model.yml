# It was generated by dbt-helper==0.1.0.
version: 2

sources:
  - name: main
    database: "{{ var('projects')['main'] }}"
    tables:
      # dbt source ID: `source("main", "main__complex_model")`
      - name: main__complex_model
        identifier: complex_model

        description: |
          Raw view complex_model

        config:
          # see also https://docs.getdbt.com/reference/resource-properties/freshness
          freshness:
            warn_after:
              count: 24
              period: hour # minute | hour | day
            error_after:
              count: 36
              period: hour # minute | hour | day
            # filter: "DATE(event_time) BETWEEN (CURRENT_DATE() - 1) AND CURRENT_DATE()" # if partition table
          tags:
            - raw_data

          # meta will be written into BigQuery labels by dbt-helper.
          meta:
            labels:
              owner: test_team

        columns:
          - name: id
            description: ""
          - name: person_struct
            description: ""
          - name: int_list
            description: ""
          - name: string_list
            description: ""
          - name: string_int_map
            description: ""
          - name: color_enum
            description: ""
          - name: ts
            description: ""
          - name: dt
            description: ""
          - name: inv
            description: ""
